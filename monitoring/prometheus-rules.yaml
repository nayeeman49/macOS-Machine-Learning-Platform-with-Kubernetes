groups:
  - name: training-alerts
    rules:
      - alert: HighLoss
        expr: loss > 2
        for: 1m
        labels:
          severity: warning
        annotations:
          description: "Training loss is high"
